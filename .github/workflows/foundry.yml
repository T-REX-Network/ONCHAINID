name: Foundry

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

env:
  HUSKY: 0
  ALCHEMY_KEY: ${{ secrets.ALCHEMY_KEY }}

jobs:
  check:
    strategy:
      fail-fast: true

    name: Foundry project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          persist-credentials: false

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: stable

      - name: Show Forge version
        run: forge --version

      - name: Configure access to private OpenZeppelin accounts repo
        run: git config --global url."https://x-access-token:${{ secrets.OZ_ACCOUNTS_TOKEN }}@github.com/".insteadOf "https://github.com/"

      - name: Install dependencies
        run: forge soldeer install

      - name: Run fmt
        run: forge fmt --check

      - name: Run build
        run: forge build --sizes

      - name: Run tests
        run: forge test -vvv

#      - name: Run coverage
#        run: ./.github/scripts/coverage.sh
